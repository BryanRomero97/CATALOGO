server:
  port: 8090

spring:

  application:
    name: catalogo
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/catalogo
    username: root
    password: tupassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect